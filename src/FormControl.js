const FormControl = (props) => {
  const { control, ...rest } = props;

  switch (control) {
    case value:
      
      break;
  
    default:
      return null;
  };
};

export default FormControl;
